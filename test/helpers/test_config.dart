import 'package:flutter_test/flutter_test.dart';

/// Default test configuration.
/// Call [setUpAll] from your test main() if you need global setup
/// (e.g. FlutterError.onError, timeouts).
void setupTestConfig() {
  // Optional: suppress Flutter framework errors in test output when expected
  // FlutterError.onError = (details) {};
}
