# Example GitHub Actions workflow for Flutter (unit, widget, integration tests).
# Copy to .github/workflows/flutter_test.yml and adjust as needed.
#
# name: Flutter Tests
# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]
# jobs:
#   test:
#     runs-on: windows-latest  # or ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: subosito/flutter-action@v2
#         with:
#           channel: 'stable'
#       - run: flutter pub get
#       - run: flutter analyze
#       - run: flutter test
#       - run: flutter test integration_test -d windows
#         # Use -d chrome on ubuntu if no Windows needed
