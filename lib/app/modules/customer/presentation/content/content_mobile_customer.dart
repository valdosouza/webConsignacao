import 'package:flutter/material.dart';

class ContentMobileCustomer extends StatelessWidget {
  const ContentMobileCustomer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Content Mobile Cliente"));
  }
}
