import 'package:flutter/material.dart';

class ContentTabletCustomer extends StatelessWidget {
  const ContentTabletCustomer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Content Tablet Cliente"));
  }
}
