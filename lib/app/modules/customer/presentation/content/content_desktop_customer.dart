import 'package:flutter/material.dart';

class ContentDesktopCustomer extends StatelessWidget {
  const ContentDesktopCustomer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Content Desktop Cliente"));
  }
}
