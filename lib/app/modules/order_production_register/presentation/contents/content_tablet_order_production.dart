import 'package:flutter/material.dart';

class ContentTabletOrderProduction extends StatelessWidget {
  const ContentTabletOrderProduction({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Content Tablet Order Production"));
  }
}
