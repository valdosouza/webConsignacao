import 'package:flutter/material.dart';

class ContentMobileOrderProduction extends StatelessWidget {
  const ContentMobileOrderProduction({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Content Mobile Order Production"));
  }
}
