import 'package:flutter/material.dart';

class ContentMobileCollaborator extends StatelessWidget {
  const ContentMobileCollaborator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Content Mobile colaborador"));
  }
}
