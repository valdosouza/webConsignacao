import 'package:flutter/material.dart';

class ContentTabletRegionRegister extends StatelessWidget {
  const ContentTabletRegionRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Região - Tablet"));
  }
}
