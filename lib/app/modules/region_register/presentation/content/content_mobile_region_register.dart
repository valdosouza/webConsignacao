import 'package:flutter/material.dart';

class ContentMobileRegionRegister extends StatelessWidget {
  const ContentMobileRegionRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Região - Mobile"));
  }
}
