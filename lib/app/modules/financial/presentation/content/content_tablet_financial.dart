import 'package:flutter/material.dart';

class ContentTabletFinancial extends StatelessWidget {
  const ContentTabletFinancial({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Content Tablet Financeiro"));
  }
}
