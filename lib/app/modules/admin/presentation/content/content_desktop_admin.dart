import 'package:flutter/material.dart';

class ContentDesktopAdmin extends StatelessWidget {
  const ContentDesktopAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Content Desktop Admin"));
  }
}
