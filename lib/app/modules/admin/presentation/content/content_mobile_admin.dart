import 'package:flutter/material.dart';

class ContentMobileAdmin extends StatelessWidget {
  const ContentMobileAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Content Mobile Admin"));
  }
}
