import 'package:flutter/material.dart';

class ContentTabletSales extends StatelessWidget {
  const ContentTabletSales({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Cadastro Sales - Tablet"),
    );
  }
}
