import 'package:flutter/widgets.dart';

class ContentMobileSales extends StatelessWidget {
  const ContentMobileSales({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Cadastro Sales - Mobile"),
    );
  }
}
