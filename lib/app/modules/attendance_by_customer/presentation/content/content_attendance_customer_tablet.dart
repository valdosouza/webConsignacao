import 'package:flutter/material.dart';

class ContentAttendanceCustomerTablet extends StatelessWidget {
  const ContentAttendanceCustomerTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Content Tablet AttendanceCustomer"));
  }
}
