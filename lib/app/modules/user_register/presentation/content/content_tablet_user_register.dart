import 'package:flutter/material.dart';

class ContentTabletUserRegister extends StatelessWidget {
  const ContentTabletUserRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Cadastro User - Tablet"));
  }
}
