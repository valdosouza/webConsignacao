import 'package:flutter/material.dart';

class ContentMobileUserRegister extends StatelessWidget {
  const ContentMobileUserRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Cadastro Usuário - Mobile"));
  }
}
