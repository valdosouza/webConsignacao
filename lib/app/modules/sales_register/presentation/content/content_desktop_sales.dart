import 'package:flutter/material.dart';

class ContentDesktopSales extends StatelessWidget {
  const ContentDesktopSales({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Cadastro Sales - Desktop"),
    );
  }
}
