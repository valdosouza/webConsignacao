import 'package:flutter/material.dart';

class ContentTabletProductRegister extends StatelessWidget {
  const ContentTabletProductRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Cadastro de Produtos - Tablet"));
  }
}
