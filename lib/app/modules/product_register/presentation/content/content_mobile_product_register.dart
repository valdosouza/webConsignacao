import 'package:flutter/material.dart';

class ContentMobileProductRegister extends StatelessWidget {
  const ContentMobileProductRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Cadastro de Produtos - Mobile"));
  }
}
